# Research

Experiments and their results. Each document is a question, what was run to
answer it, and what the answer turned out to be - including when the answer was
"do not build this", which it often is.

Design documents live in [docs/](../docs). This folder is for things that were
*measured*, with the harness that measured them, so a result can be re-run
rather than believed.

| | |
|---|---|
| [models.md](models.md) | **measured** - would trees, forests, cosine similarity or an MLP help; a 1KB logistic regression beats all of them, and five times the data did not change that |
| [features.md](features.md) | **measured** - `side` alone matches all nine features together, generated features make it worse, and the trivial "level holds" rule still beats our directional call everywhere but the top decile |
| [aligning.md](aligning.md) | **measured** - trading against a live higher timeframe costs 2.5 to 4.1 points of held rate at 3m and 5m, nothing at 1m, and reverses at 15m; the pooled figure says the opposite and is Simpson's paradox for the third time |
| [generated.md](generated.md) | **measured, two nulls** - the Deriv synthetics share no driver (325 pairs, the largest correlation is between two unrelated instruments), and FOCuS detections land on origins at *below* chance at every threshold from 1 to 20 nats. **Not even briefly** (2026-09-24, `comovement.py`): window by window at 3m/15m/1h over 64 synthetics, the largest eigenvalue and strongest pair match a shift-scrambled null (0-2.7% of windows beyond its 99th percentile against 100% for the FX control), and a pair's correlation in one window predicts the next at +0.001 against FX's +0.85 - the short-lived co-movement is chance, and pairs trading on the synthetics is closed |
| [twins.md](twins.md) | **measured, null, and one exact positive** - `Volatility 75` and `Volatility 75 (1s)` are not one path seen at two rates: over **916,248 comparisons** on 1.1M ticks and 59 days of bars, no twin correlation, tail coincidence, volatility clock or tick-index alignment stands outside what mismatched pairs of the same instruments produce. What does hold is the stated law - all twelve volatility indices are within **two standard errors** of their advertised annualised volatility and pass every iid-normal test applied, and a twin pair carries identical volatility *per second* with per-tick variance differing by exactly sqrt(2). Also: all sixteen synthetics publish on one clock to within 9ms, which fakes a correlation of +0.19 in |return| on a fixed grid, and `volatility_150_1s_index` is quoted too coarsely to carry any tick statistic |
| [generators.md](generators.md) | **measured** - the Deriv synthetics are exactly the generators the venue publishes: realised volatility equals the name on 12 of 12 indices within **0.49%**, Step Index is a fixed 0.1 step and a fair coin to **p=0.499734 +- 0.000220** over 5.2M flips, Boom/Crash spike at the stated tick rate with a memoryless arrival on all six, and Range Break 100 breaks **2.066x** as often as 200. There is no volatility clustering anywhere (largest |acf| 0.017 against 0.219 on the real feeds), so the published constant beats the rolling estimator on all 24 instrument-halves and the estimator's correlation with the next move sits in **[-0.015, +0.007]** against 0.366-0.493 on the real feeds. Nothing is directional. The one thing that costs money is that a stop on the spike side of Boom or Crash fills **10 to 19R** past where it was placed |
| [deriving.md](deriving.md) | **derived, and the theorem is the result** - the synthetics' parameters are published, so the barrier questions this desk answers with estimators have closed forms. A predictable position on a martingale has zero gross expectancy, so **`E[net] = -(c/2) x turnover`** for every stop, target, trail, grid, entry filter and sizing scheme - verified 23 ways on two paths, max |z| **1.84**, against a look-ahead control earning **+0.5002 per unit of turnover where every honest rule pays -0.5000**. The closed forms need the **0.5826** discrete-monitoring constant and then they are exact: a 1:3 barrier is a **30.7%** shot rather than 25% on 174,501 trades, expected durations land within **0.1-1.9%** where the textbook forms are **12-178%** out, and the same constant derives generators.md's unexplained **0.870 / 0.909** Parkinson ratio to **0.30%**. Boom/Crash's **+13.96R** stop slippage is now a formula reproducing all 24 measured cells to within 5.2%, and the grind side wins **98%** of ten-tick holds at an expectancy of exactly zero. Range Break turns out to have a real range - sub-diffusive by **3.6x** and **8.9x** between breaks - and the break pays for it exactly. The Jump **1.322** is **not** in the spread: `c/sigma` is **1.0103 +- 0.0151** against *realised* volatility on all five, so the venue already knows. And `volatility_250_1s_index` costs **289 seconds** of its own volatility to cross, against 1.0 for the Jump family |
| [cascading.md](cascading.md) | **measured, four tests, two positives** - volatility does cascade coarse-to-fine on all fourteen scale pairs and in both halves of a split, but the effect is +0.01 to +0.03 of correlation and it is a *real-market* property: synthetics score +0.004 against metals' +0.039. The monofractal discriminator is confounded by the marginal - an i.i.d. Student-t with no cascade scores higher than any real instrument - but 13 feeds survive the control and the 12 Volatility indices are **exactly Brownian**, H = 0.50 and kurtosis 3.00. Tails thin, and `MAD_TO_SIGMA` is the Gaussian ratio where FX runs **27% above it at 1m**. The flow/liquidity ratio is null three ways, the sharpest being that 24 of 53 feeds emit one quote a second by construction |
| [starving.md](starving.md) | **measured, and fixed in two halves** - 23 OOM kills in ten days. The *periodic* cause was the save, and streaming it took the transient from 0.394GB to **0.000GB** byte-identically; the plan was right about the fix and wrong about where it was, since 97.1% of a 206MB state is three pickle blobs and streaming the codec's tree was streaming 2.9% of it. The *structural* cause was that the state held **349 feeds for a 53-instrument book**, because `PRICES_CCXT_TOP` bounds what is discovered at one moment and the top-25 by volume rotates. Three earlier diagnoses on this page were wrong and are kept |
| [compounding.md](compounding.md) | **measured, and the answer is a number of trades** - what turning an edge of size X into a large account costs, simulated by resampling the live R distribution rather than assuming one. The book is **-0.1354R over 398 closes, CI [-0.208, -0.060]**, so the growth-optimal fraction is **exactly zero** and the shipping 0.25% ends at 0.711x over 1,000 trades. Given an edge, the safe fraction is **an eighth to a quarter of Kelly**, not a half - quarter Kelly still halves the account before the target one time in five - and a tail-shaped edge sizes at **42%** of a broad one of the same mean while doubling ruin at the same fraction. A broad **+0.05R** compounds 100x in 1.2 years at f=0.0129, which makes the goal arithmetic rather than hope; the binding constraint is that confirming +0.05R takes **1,600 trades** against 398 on the whole record |
| [edge.md](edge.md) | **measured, and it is the question everything waits on** - all 688 closes on record including the 290 the journal was losing, keyed by strategy. `sweep-aware` is **+0.154R, 95% CI [-0.022, +0.346]**: right sign, positive in both halves, survives losing its best trade, and **not yet distinguishable from zero**. `confluence-scalp` is confidently negative and was switched off. 33 more closes settle it |
| [spending.md](spending.md) | **measured, and it splits the book in two** - 43% of closes are on instruments `deriving.md` proves cannot pay, and pooling them with real markets hides what the desk is actually doing. On real markets the book is **-22.2% of the risk it deployed, 95% [-36.7%, -7.5%]** over 133 closes, and the sign survives the restriction, the five worst closes and every cut. The geometry is the finding: a **40.6%** hit rate is **1.7 points** off break-even at the payoff the book *plans* (1.36:1) and **18.4 points** off at the payoff it *realises* (0.70:1), so the exits are the loss and the entries are close to adequate. Wins come in at 54% of their risk where losses come in at 78%. `snap` is the one strategy whose interval excludes zero - **-34.3%, [-65.2%, -2.0%]** over 29 closes, a 1:1 payoff with 114 seconds to find it, which cannot clear two crossings of the spread. The generated half's own per-risk figure **flips sign** under the same restriction and is withdrawn rather than reported |
| [implied.md](implied.md) | **measured, and the first thing here to beat persistence** - VIX does not add to our own volatility estimate, it *replaces* it (`both` is no better than `vix` alone), beating the naive baseline by +0.26 to +0.43 over 15 years walk-forward. The term structure adds 1-2 points; VVIX adds nothing and does not lead. Scope is the equity indices at a day and up |
| [gamma.md](gamma.md) | **measured** - option expiry suppresses index volatility on all four equity indices (0.865, 0.921, 0.909, 0.768 of base) while the placebo second Friday sits flat at 1.004/0.958/1.004/0.981; quarterly triple witching pins harder on all four and the two that pin hardest rebound after. Three of four survive a 2018 split, ^DJI does not. The positioning half - open interest, dealer gamma sign - cannot be tested from free data at all and needs a collector running forward |
| [instruments.md](instruments.md) | **measured** - what each instrument earns; net -746 over 392 closes, the book's best-looking instrument is **one +622.63 trade with no R recorded**, only three instruments with 20+ closes are positive, and Boom 1000 at -0.595R against Crash 1000 at +0.053R is the one asymmetry worth chasing |
| [giveback.md](giveback.md) | **measured twice** - first when `best_r` was a constant zero and the investigation was about why, now that it works: sweep-aware keeps **27%** of its high-water mark and confluence-scalp gave back **1.758R a trade**. The cause is arithmetic - an uncapped wick-widening rule puts the trail 1.33R behind a peak whose median is 0.445R, so on nine trades in ten neither the trail nor the break-even can engage at all |
| [convexity.md](convexity.md) | **measured, and the thesis does not survive** - is this book's money in the tail. In **dollars** yes, 57.3% of gross profit in the top 5% of closes and 30.2% of it in one trade; in **R** no - 35.7%, which is *inside* what a normal distribution with the same moments gives, and the largest R on 325 closes is +2.429. Re-priced at one flat size the top 5% carry 34.9%: the tail is the position sizer. A deliberately convex exit (half stop, no target, 3R trail, a day to run) loses in discovery and wins in verify, and its whole magnitude is the **stop denominator** - divide the stop by three and the mean moves by three, in whichever direction that window already pointed; at a constant stop the shape is worth +0.009R. The tail cannot be selected for (largest of 25 features clears its control by 0.001 and flips sign in verify). And the Boom/Crash spike asymmetry is **manufactured by the stop**: 0.04R with no stop, 0.32R with one, 0.94R with a half-width one, scaling monotonically with the size of the jump the replay is filling through |
| [exiting.md](exiting.md) | **measured, and two look-aheads of my own** - replaying sweep-aware's entries under 18 exit policies; the first version reported a policy at +1.335R against +0.344R, better on 63.5% of the same trades, holding in both halves of a split sample, and it was a look-ahead. Corrected, **none of the eighteen beats what ships by more than noise**, and the replay disagrees with the live exit mix badly enough (98% stop against 81% timeout) to be evidence about the harness. `vol_bps` as a regime turns out to be the spread and nothing else |
| [winning.md](winning.md) | **measured, null** - what separates a winning trade from a losing one on 685 live closes. Against a permutation control run over the whole scan, **nothing the desk knows at order time clears the noise floor on R**: the largest gap across 43 features is 0.433 where shuffled outcomes produce a median of 0.374, and 24 of 43 features survive a split sample where noise survives 22. The one survivor was pre-registered by `exiting.md` - spread as a share of the trade's own **risk**, worth +0.369R cheap-minus-expensive, p<0.001 permuted within feed, 9 of 9 strata - and the gate that ships measures it against the **reward** instead, so 49 trades passed it and lost 0.471R each. Holding time looked like the strongest separator, got *more* significant when the missing half was added, and is entirely the stop rate: remove stops and it reverses at p=0.63 |
| [lagging.md](lagging.md) | **measured, null, and the control is the finding** - is Deriv's crypto quote late enough behind Binance/Bybit/the exchanges to trade. It is late, by +1.3bps of mid on btc against a placebo flat at 0.05 - and so is every exchange: put each venue in the broker's chair against a consensus excluding it and Deriv is **third of six** on btc, beaten by Bitstamp on two feeds of three. What is Deriv-specific is the spread, 2.39bps against Binance's 0.001, so the same catch-up that pays five venues costs Deriv -1.22bps a round trip. The market-made subset does pay at zero delay (+1.54bps) and is negative by **half a second**. Also: every quote timestamp is our own receive clock, 44% of Binance's rows land in one decile of the second, and `prices.db` is not corrupt - only its `quotes_feed_ts` index is |
| [reacting.md](reacting.md) | **measured, null** - no seconds-scale edge after a call: the excursion against a matched control sits between -0.015 and +0.003 volatility units at every horizon, while the median spread is 0.159v |
| [crossing.md](crossing.md) | **measured, three nulls and one candidate** - the cross-venue and cross-rate hunt for a *structural* edge. `dev_bps` reverts **less** than a permutation of itself in 20 of 22 cells, by less than the deviating venue's own spread, and 90.2% of deviations belong to a venue the desk cannot hold. Seven of eight FX triangles are tight to 0.08bps against a 0.85-1.54bps three-leg cost, and their sixty-day "one-signed offsets" are predicted to within 0.045bps by bid-versus-mid arithmetic. No instrument leads another: the second-largest off-zero correlation in 336 is **us30 against btc**, beating six of eight related pairs, and the largest implied move anywhere is 0.089bps against a 2.35bps spread. The twelve Volatility indices realise their own names to within 0.5% in both halves. What survives is **CADJPY**, whose price disagrees with the broker's own USDJPY/USDCAD by +0.41bps and by +1.23bps every Thursday - the T+2 value-date roll, nine Thursdays of nine - a locked three-leg trade worth +0.69bps a week that lives or dies on three swap rates nobody has read. Also: `lp_time` is collected and discarded, so no cross-venue lead-lag is measurable from what is stored |
| [forecasting.md](forecasting.md) | **under revision** - the comparisons were made while two volatility conventions were mixed asymmetrically, so the two headline claims are probably artefacts; what survives is that persistence's edge shrinks monotonically with horizon, and a catalogue of three clean tables that were all defects |
| [volatility.md](volatility.md) | **measured** - the estimate is well calibrated and its half-life is well past the optimum; a flat 20-bar mean beats it at every interval |
| [resolution.md](resolution.md) | **measured on production** - two thirds of outcomes resolved within two seconds; a bar's wick was resolving touches born inside it, and no bars-only replay could see it |
| [states.md](states.md) | **measured** - does a level's behaviour change over its life; it does not, recency predicts worse than pooling, and there is no flip |
| [structure.md](structure.md) | **measured** - the transit graph, confluence and the shape of the level set; the graph is flat and the one strong-looking property is `side` wearing a distance |
| [magnitude.md](magnitude.md) | **measured** - does it know how far, and what being wrong costs; `expected_push` ranks profit 7.5x and the `reward_to_risk` gate inverts the sign of the return |
| [prior.md](prior.md) | **measured** - what `edge` is actually measuring; subtract a side-aware baseline and the level's record plus its neighbours predict at 51.8%, AUC 0.520 |
| [turns.md](turns.md) | **measured** - can a major turn be seen coming; yes, weakly. AUC 0.595 purged over 310 turns, and `vol` alone carries it |
| [cycles.md](cycles.md) | **measured** - does a level's place in the larger move matter; one cell separates by nothing at all, and the AUC gain's interval includes zero |
| [news-models.md](news-models.md) | survey - model families that could turn stored headlines into features, and which of them fit in 640MB beside everything else |
| [reading.md](reading.md) | notes - AlphaGo Zero, Llama 3, InstructGPT and DeepSeek-R1, what transfers and what does not; all four guard against the reward hacking this system did to itself. Plus the microstructure papers behind levels, VWAP, open interest and funding - including Osler's finding that **round numbers predict as well as published levels**, which is the control this desk has not run |
| [localising.md](localising.md) | **measured** - the origin survives its own falsification test, the fine-resolution estimator halves its wander, the body-edge rule adds nothing, and a band re-derived at 1m is 2.6x narrower for 94.8% of the same returns: shifting the 15m bar grid by a minute moves it a median 0.23v, 2.8x steadier than a matched random book and about a seventh of the band it already carries. Includes the corrected harness bug and three discrepancies between the origin-localization specification and the code |
| [locking.md](locking.md) | **measured** - the "resolves in two seconds" figure described a bug that was fixed; only 3.8% do now and the median is 191s. The give-back is real but it is the trap, not the clock: a trap keeps 34% of what it reaches, a break keeps all of it. And the scale-out never fires because the trigger reads the current price while the record keeps the high-water mark |
| [choosing.md](choosing.md) | **measured** - 536 counterfactual returns say the three strategies with real samples are indistinguishable at -0.17 to -0.19R and every strategy is negative at -0.216R pooled, so a selector would choose between options not shown to differ |
| [detecting.md](detecting.md) | **measured** - KSWIN, DDM/EDDM and a hand-written FOCuS added beside ADWIN, all deciding nothing; against their own nulls EDDM alarms up to 11 times per 3,000 on a model that has not changed and FOCuS at most 5 across five such streams |
| [spiking.md](spiking.md) | **measured** - the Boom spike is visible in stored 1m bars, one every 11.9 minutes, and its waiting time has a coefficient of variation of 0.99 against a memoryless process's 1.00. Nothing can call it; what survives is arithmetic on a nearly deterministic grind |
| [trapping.md](trapping.md) | **measured** - traps are 64% of all attempts on a level and 94.6% on 30m; predicting trap from break within a timeframe is AUC 0.567 on the level's own record, and the pooled 0.6187 was the timeframe rather than skill |
| [rounding.md](rounding.md) | **inconclusive, and the reason is the finding** - Osler's round-number control over 16 instruments; drawn, round and random books all hold price within one standard error, but the positive control fails too, so a 5m audit cannot resolve a book whose outcomes resolve in seconds |
| [swinging.md](swinging.md) | **measured** - `swing-level` replayed over 17 instruments and 1.1M bars; the candle gate pays, the 1.5 stop was what lost, and every cell with a 1.0 stop is profitable |
| [bandits.md](bandits.md) | design note - where a bandit fits (attention budgets, not the alert gate) and why gymnasium is not the reason to reach for one |
| [positioning.md](positioning.md) | what supply and demand this system has - six ideas inferred from price, the macro block that is all supply and no demand, and the CFTC positioning that is the one *observed* measure reachable from here |
| [catalogue.md](catalogue.md) | **measured** - which of the broker's 798 symbols are worth carrying; in volatility units the synthetics cost 0.170v to cross against FX's 2.267v, which is the opposite of what the point spreads say |
| [stops.md](stops.md) | **measured on production** - stops are the whole loss; 20 of 27 judgeable stopped trades never reached target and 7 were stopped early, so the answer is a better entry rather than a wider stop |
| [strength.md](strength.md) | **measured, not built** - how strong is a level and does it matter; the strongest single thing a level knows |
| [similarity.md](similarity.md) | **measured** - does `Features.distance` order neighbours; at tradable horizons AUC 0.4921 and 0.5120 against a 0.500 control, and the fast buckets that look decisive are a tautology - `side` fixes direction 100.0% under a minute and 52.8% beyond thirty |
| [handoff.md](handoff.md) | **start here** - what is true, what is broken, what cost time, and the tautology that reframes every model score in this repository |
| [force.md](force.md) | **measured** - does the force arriving at a level decide whether it breaks; it does, AUC 0.560, and depth into the zone separates harder still at 0.599 inverted, while `up_rate` - the best direction feature there is - predicts breaks not at all |
| [paying.md](paying.md) | **measured** - does the five-to-thirty-minute edge pay the spread; the synthetic null scores 47% where real instruments score 70%, and 11 of 26 clear their own cost with gold and spx500 an order of magnitude ahead |
| [barriers.md](barriers.md) | **measured** - the two-level trade as a triple-barrier problem; on 40,803 replayed setups no interval reaches the far barrier more often than the stop, and the +0.66v that survives comes from a 3.5:1 corridor rather than from a hit rate |
| [timeframes.md](timeframes.md) | **measured** - do higher-timeframe levels hold better; a 1m level breaks sixteen times more often than a 1h one, monotonically - but breaking less is not being more tradable, and 1h arrives twenty-seven times slower |
| [horizon.md](horizon.md) | **measured on production** - the level model's edge is +45% on touches resolving inside five minutes and **+0.00%** beyond thirty, which is the horizon `max_hold` trades; and 27% of resolutions carry a negative duration |
| [learning.md](learning.md) | **measured** - is the kNN behind every level call buying anything; at a matched 1,996 touches it beats a one-feature floor by 3.1 points of edge, a learned distance adds nothing over it, and a logistic regression on nine features matches reading one |
| [formations.md](formations.md) | seven ways to find a level and why they run together; 57% of levels now have more than one method behind them, where every one used to be `pip` alone |
| [shelves.md](shelves.md) | **measured** - do high-activity bands get respected; pooled over 849 windows a node is reached 45.8% against a control's 46.3%, and the synthetic null reads -11.6% |
| [origins.md](origins.md) | **measured** - do origins hold when price comes back to them |
| [reachable.md](reachable.md) | **measured** - how often all of `origin-swing`'s conditions hold at once |
| [geometry.md](geometry.md) | **measured** - where reward-to-risk actually comes from, and why a floor on it keeps losers and refuses winners |
| [lateness.md](lateness.md) | **measured** - what entering late costs |
| [macro.md](macro.md) | design note - monetary policy as features on a signal and as a model of its own, and why a rate differential needs both legs from one series family |
| [rebuilding.md](rebuilding.md) | **rebuilt, and two corrections to [deriving.md](deriving.md)** - the other half of the derivation: a simulator per family from the published parameters alone, ticks aggregated to bars the way the feed is, then an adversarial classifier asked which is which. **103 published numbers regenerated with no database**, eleven fired. The barrier table comes back to **+0.0004 on P(up)** where the textbook form misses by 0.057, and the **0.5826** constant and the **0.870 / 0.909** Parkinson ratios fall out of a rebuild given a volatility and a tick rate. Two things do not: the published `lambda`, `E[g]` and `E[J]` **miss their own closure by 6-12% on all six feeds**, so a Boom built from them is not a martingale until `E[J] := lambda*E[g]` is imposed; and a lognormal on two moments leaves `P(J>D)` at 1.000 against a measured 0.963, so **the quantiles of `J` need publishing**. A **discriminator loop** then runs to exhaustion - read the largest feature, measure it, add one term, re-run - and publishes its refutations: rounding the accumulated price onto the quote grid is caught at `frac_zero` **AUC 0.686 against a floor of 0.499**, moving one unit instead overstates the one-unit bin by **45%**, and the reading both of those suggest dies on its own control. **A collector that never stores an unchanged quote makes the identical histogram**, and the share of doubled inter-tick gaps matches the predicted repeated-quote share on **twelve feeds of twelve** - which also retires this page's own "our collector dropped 0.01% to 34% of the ticks" as the same fact. On the **seven feeds whose quote grid is fine enough for a tick statistic to be about the law**, no arm beats its own floor at any sample size: 0.5040 and 0.4928 against floors of 0.5105 and 0.5019, `n*` infinite. Every other family is at or beyond the sample that exists too, after a defect in this harness was found: a stored quote arrives as `(bid+ask)/2` and a rebuilt one as `round(path/grid)*grid`, so their differences carry different floating-point error, and `n_distinct` separated **crash_500 from its own rebuild at AUC 0.024** where the same feature on lattice-snapped increments reads **0.502**. Corrected, Step Index, Range Break 200 and Crash 500 are unseparable on both arms and Boom 300 needs **6.7x** the store. **Boom and Crash replicate and their published parameters do not**: the feed's own marginals are at the floor on **24 arms of 24** where the five published numbers are caught on **24 of 24** at 724 to 3,506 ticks. The grind is why - `E[g]` and `CV[g]` pin a gamma of shape 2.2 whose density is zero at the origin, and the feed's grind is **flat** there, so a third amendment joins `E[J] := lambda*E[g]` and the quantiles of `J`: **the quantiles of `g`**. Also measured: not one odd half-grid move in six Boom/Crash feeds happens without a spread change, so the grind's lattice is **0.001 where `quote_grid(mid)` reports 0.0005**. **Can the next tick be predicted? No, and two of the five rungs had no power to ask** - LLL recovers all nine published LCGs from their own controls and **none of 1,080 attempts** on the feed; MT19937 untempering needs 32-bit words against **13.89 bits a tick**; the joint-stream firings at p = 6.8e-64 are the publication clock, and fifteen independent PCG64 rebuilds on the same slot mask fire harder; and the forward classifier's own LCG control reads **0.4998**. **The shared Range Break width of 60 is withdrawn**: given a simulated truth the direct estimator returns the width it was given to within 12% and a true 60 reads 53.1 +- 2.4 where the feed reads 37.8, while the curve this page fitted cannot separate 52 from 60 and is dominated by an `n=1` cell **no hard box reaches** |
| [quantising.md](quantising.md) | **measured on the synthetics, and three of its own published numbers corrected** - the Wick rotation makes each generator a textbook quantum system, so propagators, boundary conditions and spectra are already statements about these prices. The **0.5826** is the *extrapolation length* of a discretely monitored wall - a Robin condition, one image charge moved - recovered at **0.58226 ± 0.00058** and **flat in barrier distance from 2 to 12 sigmas**, with the moved plane 50-70x better than the textbook one; the same propagation returns **0.3073** for deriving.md's 3:1 barrier against the 0.307 it measured on 174,501 trades. **Range Break is not a box**: the measured `lambda_2/lambda_1` is **2.076** (RB100) and **1.976** (RB200) against simulated boxes at 3.197 and 3.559 through the identical pipeline, so 1:4:9 is excluded at **3.4 and 4.2 sd** and the well is at or below harmonic, `p <= 2` - which refutes rebuilding.md's soft edge *below* the interval two pages predicted. Confinement itself comes from the knee (**-0.24** against a free walk's -0.77) and the AR(1) half-life (**164 ticks** against step_index's 11,176), because the free-walk control covers the ladder. The width **saturates at 38 on RB100** and has not converged on RB200 (>60), so the shared 60 is refuted; the volatility_75 control climbs 74% over the same cuts and never plateaus, which is what makes the plateau readable. The reconstruction is the part with money in it, and its classical control is an **identity** - Kalman, Brownian bridge and linear interpolation coincide to `7.1e-15` - so all the content is what the high and low add: against a **known true tick path** the attainment ancilla removes **39-48%** of the interior variance linear interpolation leaves, containment 26-29%, and attainment beats containment by **+10.36%** against the +9.88% this page had costed it at. **The Wigner function is negative and every case of it is the quote lattice** - `cos(dk)^n` at the Brillouin zone edge with exact odd/even parity, 18 of 18 cells parameter-free - which bounds the analogy: nothing here needs a quantum state. Pointed at the desk's own code the answer is mostly **no**: `H` and `L` are the extremes exactly (0.000e+00 over 34,542 bars), so wick statistics, single-barrier hit tests and the range family are already exact and cannot be improved; Garman-Klass beats every reconstruction on sub-bar volatility by a factor of two, because a conditional mean is smooth. What does pay is **`replay.py`'s Rule 3**, "the stop is checked first" - a coin flip at 0.47-0.68 accuracy against the reconstruction's **66-93%**, costing a one-sided **9 to 44 points of risk per resolved trade** in the trailing regime, which calibrating.md makes visible at 56-1,400 closes and which is the size of the largest claim on spending.md's strategy table |
| [grounding.md](grounding.md) | **derived, and five of the six derivations lose to their own classical control** - can a learning algorithm be derived from the generator rather than borrowed. The box ladder `1:4:9` and the spring's `1:2:3` are **one formula at two densities** - Rayleigh-Ritz on a Fokker-Planck Dirichlet form - recovered to five decimals with no transition data, which makes `quantising.md`'s dynamical ladder a prediction the histogram can contradict; on RB100 the density reads **1.911** against a simulated spring's 1.913 and separates box from spring at a single-cut error of **0.003** where the transition matrix manages 0.025, but a Kramers-Moyal generator manages **0.000**. What is new is **`R`**, the ratio of the two estimates, which is 1.00 on a diffusion, **2.0 on independent data** and 3.18 when the marginal is a scale mixture the dynamics do not share. Pointed at `barriers.py`: the closed form is **best of five arms at all ten geometries** on tier-1 feeds, wrong by **55.7 standard errors** on Boom, and understates the far barrier by **+0.83 points [0.52, 1.13]** at 3:1 on real feeds, mirror-antisymmetrically, 17 of 19 - and the feed's own return histogram removes **96%** of it where a learned volatility operator removes none. The number with money attached is that **`barriers.duration` is short by 25% at 3:3 and 96% at 10:10 on 19 of 19 real feeds** against 5-9% on tier 1, confirmed independently at **tick resolution** on a live pull (1.18-1.49x, controls at 0.99-1.08) with a mechanism whose sign flips - **BTCUSD, the one feed with positive tick autocorrelation, is the one that resolves faster**. And four ceilings: a real feed's past and future share **one** degree of freedom, TICA loses to PCA at every rank, a **flat 20-bar mean captures 95.6%** of what eight constructions reach, and a GRU's recurrent Jacobian reads `tau_1` of 1.5-15 steps against truths from 4 to 400 where Ulam gets every one to within 10% |
| [families.md](families.md) | **measured on 11.34M ticks across 43 symbols, and the first non-martingale on this book** - the study of the synthetics that are *not* Volatility indices, ranked by how much structure a sequence model actually finds. **Drift Switch is genuinely predictable**: `acf1 = +0.1525` at **t = +34.1** on 50,000 15m bars, halves +0.1552/+0.1499, shuffle -0.0030, every control symbol under |t| = 5.2, and **zero at tick resolution** (-0.0065 +- 0.0022) which rules out a bar-construction artefact. It is also **entirely linear** - a phase surrogate reproduces 96% of it, a GRU (0.5346) loses to an HMM filter (0.5449) and the surrogate reproduces both - and **it does not pay**: an AR(1) reaches 56.9% (63.1% on DSI 10 at 3m) and clears only **0.103 to 0.692 of the quoted spread** in all nine significant cells, because the signal lives at 3m-1h and the cost advantage at 4h+ and they do not overlap. **Boom and Crash are memoryless on a sample that could have said otherwise** - 300,000 ticks x 29 feeds at 303-5,462 spikes each against `generators.md`'s 86-306 on six, five statistics each against 200 compound-Poisson replicates through the identical detector, **max |z| = 3.63 on 145 tests against a Bonferroni 4.16**, and `min_gap` = 1 tick on 16 of 29 so there is no refractory period; the name is the rate on all eighteen (1.014 and 1.031), including the twelve never measured. **Skew Step's name is exactly true and the pre-registration was backwards in both parts** - `4 Up` is `p(up) = 0.799944 +- 0.00089` with up 0.12507 and down 0.50000, so it rises *often in small steps*, and the ratios are 4:1 and 9:1 rather than 5:1, which makes the number the **magnitude count** and not the ratio; probability and size are reciprocal to four significant figures and closure holds on all four (z = +0.04, -0.82, -0.60, +1.30). **Jump and DEX rates are detector artefacts** - they move 1.76x to 3.01x across thresholds, so the published number is not the rate, though their memorylessness survives. Three of six kill conditions fired, plus two unnumbered corrections that would each have been a false headline: a quantile-binned hazard estimator reading 0.148 on a *simulated memoryless* process, and `1/(1-p_ii)` reporting a dwell of 7.5 on shuffled i.i.d. returns |
| [sequencing.md](sequencing.md) | **measured, null, and the positive control is the finding** - recurrent nets (LSTM, GRU, vanilla RNN) on the Volatility family: 21 symbols x 8 timeframes x 3 architectures x 4 variants x 2 targets x 5 walk-forward folds, **2,464 cells** plus a 384-cell architecture sweep and a 480-cell control on real markets. **Direction calibrates**: mean AUC **0.50150** over 462 cells and **7,728,858 test rows**, and the *phase surrogate scores higher* at 0.50183; the best cell in the family (0.5801) is on **361 test rows**, lands on the **95th percentile** of the best-of-462 null at family-wise **p = 0.0511**, and the best control cell in the same band is 0.5572 - banded by sample size the controls win three bands of five, and on one symbol the AUC runs 0.504 at 41,649 rows to 0.580 at 361. **Magnitude closes against the net**: R^2 **-0.3139** on the feed, *worse than on a permuted target* (-0.2823) and worse than on a simulated GBM at the published sigma (-0.2139), beating the unconditional mean on 17 of 462 cells against the shuffle's 5 of 154 - and **a HAR line on rv1/rv5/rv22 beats it**, 0.7094 srel against 0.7462, with the mean beating both. `R2_naive` turns out to be an **autocorrelation**, `rho = (1+R2)/2`, confirmed on four GARCH processes to three decimals; measured that way **all twenty-one symbols lie inside [-0.013, +0.015]** with eight negative, against a 0.007 standard error - there is no clustering here to forecast. **The positive control passes twice**: real markets through identical code give `rho` **0.086-0.155**, HAR positive at all eight timeframes, **41 of 46** under-parameterised cells beating the mean against **8 of 107** on the synthetics, and direction AUC **0.51527** (BTCUSD 1h **0.5502** on 41,649 rows, family-wise p = 0) whose mechanism is visible in a momentum baseline **below 0.5 on all five real symbols** - bid-ask bounce, not an edge. **`Spot Up - Volatility Up Index` is the one name that comes true**: the only symbol of 21 with a positive mean HAR R^2, `rho` 0.039/0.019/0.023 at 3m/15m/1h against its own null's 0.001/-0.004/0.005 - **one percent of variance**, and at 15m the net scores -0.1646 where HAR scores +0.0079. Two defects fell out: **`seqlab.gbm_bars` was emitting 11.28% of bars with a high below `max(open,close)`**, impossible on a traded bar, handing any feed-versus-simulation classifier the answer from the sign of one feature on one bar in nine - it is the null **every arm of the study scores against**; and the study's own validation-selected geometry failed its own capacity rule at *every* cell, where **17 of the 18 positive R^2 cells in the sweep have more parameters than training windows**. Also measured: early stopping on a slice and reporting that slice is worth **+0.011 of AUC and +0.027 of R^2** that is not there, and `Spot Up - Volatility Down Index` **404s on all eight timeframes** |
| [controls.md](controls.md) | **the arm that makes every other negative result mean something** - the same loader, the same 28 features and the same purged folds, pointed at real markets, because a folder of nulls is worth nothing until something proves the instrument can detect a positive. **It passes in both directions at once**: rank autocorrelation of `|r|` is **+0.140 to +0.238** on the five real feeds at every timeframe against **-0.005 to +0.006** on the Volatility family and the same on simulated GBM; Ljung-Box(10) is **3,224 to 27,557** on real against a 95% critical value of 18.31, which **no Volatility index reaches anywhere** (6.3 to 14.0); a HAR ridge scores out-of-sample log-vol R-squared of **+0.32 to +0.49** at h=22 on real markets and **negative in every Volatility cell**. A leak in the shared layer would have inflated the synthetic column too, and it is flat. **It also corrects a claim this study published**: the h=1 `srel` gap does *not* invert on gold at any timeframe, because at h=1 the target is a single `|r_{t+1}|` and the naive forecast costs 0.88 even with sigma constant and known - two independent half-normals - so the gap is a very strong **discriminator** (29 sd at 1h) whose *sign* carries no information about clustering. The statistic that does invert is the **matched-window** one, `rv_h` against the mean at horizon h: **+0.044 to +0.203 on all five real symbols at all eight timeframes at h=22, and on no Volatility symbol anywhere**. Telling a real market from a Volatility index takes about **300 bars** (n* = 273-857, cheapest 374 at 4h/6h), with standard errors from a moving-block bootstrap rather than assumed. **Cross-sectionally there is no shared factor in the price process and the only shared thing is the clock**: not one eigenvalue of the returns or `|returns|` correlation matrix leaves the Marchenko-Pastur bulk at any timeframe in either universe, mean off-diagonal +0.0002, top eigenvalue carrying 4.9% against 1/21 - while the same test on **log volume** returns a top eigenvalue of **6.6 to 16.4** against a bulk edge of 1.04 and pairwise tick-count correlations to **0.9996**, which is `twins.md`'s 9ms publication clock seen from the other side. The detection floor is measured rather than claimed: a shared factor loading 0.05 would have been caught on every draw at 3m and 15m, 0.08 at 1h and 4h, and **6h and longer are reported untested rather than null**. **No twin pair couples** - the one candidate, Volatility 100 against its own 1s variant at 15m lag +3, `r = -0.0261` in both halves, is killed by a test only bar level can run: +3 at 15m is 45 minutes, which is lag +15 at 3m where the correlogram is flat and the 3m peak sits at lag -16 with the opposite sign. Two of its own methodological bugs are recorded: a short-feed filter that ran on already-aligned data and **could never fire**, studying 5,066 bars where 32,668 existed; and a family-wise null taken as a rank among 90 rather than a max over 9, which manufactured a p = 0.011 |
| [pipeline.md](pipeline.md) | **the deterministic middle of Deriv's chain, identified - and one negative promoted to a theorem** - the source is closed and `rebuildpredict.py` closed it; this is every stage after it. `dt` is **exactly 2000 ms** on Volatility 75: the measured 2000.807 is not drift but **122 missing publications** over 302,421 slots, and the arithmetic closes to the digit. Jitter is 46.5 ms sd around a **222 ms offset**, and all six probed feeds share that offset bucket across prices from 0.36 to 49,288 - `twins.md`'s shared clock reached from a third direction, and the reason the cross-sectional arm finds a factor in tick counts and none in returns. Tick sizes are measured per feed off the **bid**, because the mid sits on a half-grid whenever the spread is odd. **The rounding mode is not identifiable, and that is a theorem rather than a failure**: floor, ceil, truncate and round-half-up are all `grid * floor(x/grid + c)` for one constant, `c` is the lattice *phase*, and a continuous internal price is uniform modulo the grid - so half-up against half-even is **bit-identical** and floor, ceil and truncate sit within 1.5 standard errors on every statistic. The two tests that were briefed for it - last-digit distribution and residual sign - are exactly the two the phase argument kills, and both were run rather than argued away; it becomes identifiable only if the quantiser feeds back. **11.9 bits a tick** on the most informative feed, 5.8 to 9.0 elsewhere, so no feed emits a whole 32-bit word and `rebuildpredict.py` rung 2 is **impossible from single-feed quotes rather than merely unsuccessful**. The atom battery - the test for whether a finite-precision inverse-normal makes the achievable increments enumerable - names a lookup table's width correctly on **6 of 6 from 8 to 24 bits** against an out-of-sample false-positive rate of **2 in 32**, and the arm caught its own threshold set *in sample*, which had duly "detected" a 32-bit uniform twice in six, and recorded the fix rather than applying it quietly. Two results correct other pages: the drift convention's information is **calendar span, not bar count** - the returns telescope, so separation is `sqrt(sum sigma^2 T)/2` and a year of hourly bars says no more than a year of daily ones, which is a defect `structures/vol/projection.py` carried until it was measured; and quantisation accumulation is **decidable only below sigma_Y ~ 4** (124 sd apart at 0.65, 0.0 at 100), so Volatility 150 (1s) is the feed that can answer whether the generator rounds for display or feeds the rounded price back, and no sample size makes Volatility 75 answer it |
| [foundation.md](foundation.md) | **a pretrained financial foundation model, fine-tuned on synthetics, loses to a constant** - Kronos-mini (4.1M parameters) against the same folds and the same baselines every other arm uses. Direction AUC **0.4961** against a 0.50 floor, family-wise p 0.34. On volatility it scores srel **0.6963 against the unconditional mean's 0.6687 and loses 13 of 13** - and scores **0.6963 against a permuted target**, so it carries no information at all; log-HAR collapses onto the mean within 0.0015 and naive last-realised loses to the mean 13 of 13. **The largest deviation anywhere in the arm is on the phase surrogate**, which has nothing in it. Tokenizer look-ahead is **measured rather than asserted** - the tokenizer is causal by architecture and its quantiser is `sign(z)` on an L2-normalised projection with no fitted statistic to leak across a split, and poking a future bar by 5% moves the next-token logits at 24 earlier rows by **exactly 0.0**. The upstream example **cannot run as shipped** (it reads a CSV the repository does not contain), so the plumbing was proven on the data upstream does ship before any score was taken, and the cached sampling path is checked against upstream to 3.7e-07. Pretraining is worth about **3.6 nats a bar** before any gradient step and fine-tuning buys 0.59 more, with validation tracking training to four decimals for 800 steps at 1h - nothing to overfit; at 1d the gap grows **35x faster per step** and the page says fine-tuning there is not defensible rather than reporting its score. The model does tell the feed from a simulated GBM on entropy (3.712 against 3.930) and **predicts neither** |
| [calibrating.md](calibrating.md) | **measured, and one of this desk's own findings is an artefact** - the desk's analysis run on 2,000 null books from `calibnull.py`'s world, where `deriving.md`'s theorem fixes every strategy's true edge at minus its costs. **One `spending.py` run produces at least one interval excluding zero 63.05% +-1.08 of the time**, and in a world where every row truly runs at the book's own -22.2% it names at least one row reading `<= -34.3%` with an interval excluding zero in **95.05% +-0.49** - which is the `snap` row, so that finding is what a twelve-row cut does and not what `snap` does. The book-wide **-22.2% [-36.7%, -7.5%]** survives: 0.35% of null books reach it. The bootstrap is nearly honest (4.70% +-0.47 at n=133, 7.05% +-0.57 at n=29, and a studentised interval restores it); the table is the problem. `winning.md`'s permutation control is the one method here that lands on its nominal rate - **4.80% +-0.68** against 98.50% for the uncontrolled read - and its one survivor, `spread_over_risk` on `P(stop)`, clears in **98.30%** of null books because it is the cost rather than a pattern. An interval on `n` closes is about **330/sqrt(n)** points of risk wide, +5% of risk needs **2,400-2,600** closes for 80% power rather than the 1,000 `spending.md` quotes, and `strata.compare` on 133 closes flags a reversal in 0% of null books at its default `min_n` and 100% at any floor low enough to let it speak |
| [auditing.md](auditing.md) | **the corpus, audited** - forty-six pages judged against `calibrating.md`'s measured false-positive rates, with every cut the closes export can reproduce re-run under the correction rather than argued from. **Five withdrawals**: `spending.md`'s `snap` row; `instruments.md`'s instrument ranking (55 rows, 13 testable, **71.6%** family-wise, none survives); `aligning.md`'s alignment gate **at 3m**, which is **z = +1.78, p = 0.074** on that page's own counts and carries a live recommendation; five of the seven survivors in `exiting.md`'s twenty-one-dimension cut, controlled against three random buckets; and `spread_over_risk`. The sharpest case is **`confluence-scalp`, turned on and off within a day** on a 10-close row and a 30-close row whose bootstrap endpoints are **-0.04** and **-0.018** - a t interval covers zero on both, and the two constructions differ by 7.70% against 4.40% at these sizes. `fade-to-value` is the one row that survives correction and is the one that was left running. The folder's own discipline mostly holds: **no claim is withdrawn here because its control failed to catch something**, and `twins.md` (bar set in code before 916,248 comparisons), `cascading.md` (five control arms before any instrument was read) and `convexity.md` (counts its own comparison budget) are better than what this audit recommends. What moves is the cut table with no interval on it |
| [inert.md](inert.md) | **the catalogue** - eighteen features that shipped, configured, logged correctly and did nothing. The most common defect in this repository, and the one a result is most likely to be built on top of: `tests/test_published.py` is the gate that now fails when a published feature is constant |
| [zma.md](zma.md) | **measured, and the machinery is sound while the input is wrong** - an attention-weighted z-score with dynamic percentile thresholds, against a graded positive control. It **works**: AUC 0.518, 0.549, **0.620** and hit 0.509, 0.583, **0.705** as an Ornstein-Uhlenbeck reversion speed is dialled up. On this desk it does not - 0.493 to 0.504 on the synthetics against their own shuffles, 0.485 to 0.515 on real outrights, and **0.376 to 0.461 on Boom and Crash, which is reliably wrong**. It fires on ~15% of bars everywhere regardless, because its thresholds are percentiles of its own history. The one place it pays is **constructed cross-venue spreads at AUC 0.67 to 0.73 and hit 78.7% to 83.7%**, which reverts by arbitrage rather than by hope and which this book does not trade. A FOCuS changepoint gate raises precision from 0.8367 to **0.9103** on the best spread, but only with the inequality the other way round from the intuition: the changepoint has to be **active**, because on a reverting series the excursion is the evidence |
| [adapting.md](adapting.md) | **measured - an online mixture can say "there is nothing here", and the fixed rule cannot** - ZMA converted into four online learners, scored **prequentially** against always predicting the running base rate. A mixture over four ZMA periods **plus a null expert** puts **0.092** of its weight on saying nothing at OU theta=0.15, **0.844** at 0.05, **0.952** at 0.02, **0.960** on a shuffle of the best arm, and **0.856 to 0.998** on every family this desk trades - the confidence becomes a number the algorithm reports rather than a judgement about its output. Online learning **created signal nowhere**: eight of eleven arms come back at or below zero. The mixture's worst null-arm loss is **-0.58** milli-nats against online logistic regression's **-5.79**, so the obvious conversion is the wrong one. On OU that stops and restarts mid-series the mixture goes quiet (null weight 0.127 -> **0.922** -> 0.138) **and comes back**, which plain exponential weights cannot - their weights underflow to zero and multiplication cannot revive them, fixed by Herbster-Warmuth fixed share. The percentile threshold is a **loss, not a filter**: at an identical bet count the continuous z beats the `agrees` flag 0.562 to 0.504, 0.603 to 0.578 and **0.724 to 0.646**. And on Boom and Crash the flag is an **anti-signal with a mechanism** - hit 0.119 and 0.164, because the slow drift leaves the z-score stretched and the spike is what turns it back, so the condition fires as the drift resumes. Also found a shipped `Zma(period=200)` that kept fifty prices |
| [constructing.md](constructing.md) | **built, and the control nearly was not run** - the constructed spreads `zma.md` found an edge on now exist as ordinary feeds in the bar store, so `structures`, `vol` and `zma` read them without knowing they are derived. Measured through the shipped detector on stored bars: **mean AUC 0.6985** across eight BTC cross-venue spreads (hit 0.80 to 0.84) against **0.5031** for the same instruments as outright prices at one venue - an independent replication of 0.67-0.73 built a different way. The first outright table read 0.78 and 0.86 because the query selected on feed without **venue**, interleaving six venues' rows by timestamp, and the alternation is itself a violently reverting series. Open and close are exact; **high and low cannot be** - each leg's extreme is at an unknown instant - so the range is aggregated from a finer interval, worth **2.53x** the endpoints-only range and therefore 60% of what `har.py` would otherwise be fed. A missing leg drops the bucket rather than forward-filling, which on a difference of two nearly equal numbers *is* the signal. And the reverting kind **cannot be held**: two venues is two accounts. The holdable kind is the implied FX cross - `ln(EURUSD) - ln(GBPUSD) = ln(EURGBP)` exactly, verified at 0.85476 - which is a new instrument rather than a residual |
| [streaming.md](streaming.md) | **measured - the cost goes away, the answer does not change, and three hoped-for things are not there** - ZMA rebuilt with an exact O(1) recursion for every part. **The shipped attention was inert**: `exp(|r| - max|r|)` over returns near 1e-4 gives Kish's effective sample size **49.00 of 50** on real bars, on a spiky series and on an OU process, so the attention-weighted z-score was a plain rolling one. **Now fixed with a temperature** - and measured: it discriminates (effective n 47.99, 28.99, 6.90, 1.37 at temperatures 8, 2, 1, 0.25) and **sharper is monotonically worse wherever the detector has signal**, AUC 0.7221 to 0.6655 across that range on the best spread. Shipped at temperature 8, within 0.002 of flat everywhere: the value of the fix is that the knob is real, not that the number moved. **Nested timeframes add nothing**: three readings - dividing the mother cycle out, running with its trend, running against it - track the raw reading within **0.005** across a contamination sweep from 0 to 8 against a matched random-walk null, and mean **+0.01** on real 1m/15m/1h bars read only once closed. What does work is **how far**: the turn-depth head beats the running mean on **8 of 8** constructed spreads (+0.03 to +0.19) and on none of the simulated arms. **How long does not** - `when` scores positive on the nulls too. Two arithmetic defects found by the rebuild: exponentially weighted least squares with closed-form weight sums is **109x too large at 300 bars** and needs ~2,000 to converge, so the sums are accumulated instead; and a threshold on an unnormalised slope made `cycle_with_trend` a constant zero, caught by `test_published.py`. Same mistake as the softmax - a number needs a scale before a constant can be compared with it |
| [delivering.md](delivering.md) | **measured, four negatives and one feature set** - ZigZag pivots, FOCuS change points, perceptually important points, a change in state of delivery and this desk's own levels are **one object under several names**: the last price at which the market changed its mind, knowable only after a delay, carrying one claim - price comes back and is rejected. Run as one hypothesis with one scoring rule, always from **confirmation** rather than from the extreme. On real outrights the claim is **2 to 4 points over matched-random** (gold 0.49-0.52, eurusd 0.48-0.52) against the 63-65% and 14-23 points reported elsewhere, which does not reproduce on this sample. On constructed spreads it is 0.90-0.97 against a 0.51 control, but an extreme of a reverting series is by construction a point price left. **The 86% retracement is refuted**: median retracement is **1.00 on every arm including Brownian motion**, half of all legs exceed the one before them everywhere, and the share landing near 86% is the same on gold as on a random walk. **Depth left before the turn is what a random walk gives you** - gold 2.87 sigma against GBM's 3.04, eurusd 2.81, spx500 2.98 - with p90 three times the median, so a trail held for the tail gives away most of the distribution. **Breaking the cycle above says nothing**: controls resolve the effect at z = -6.60 where it exists and Brownian motion at zero, while every real instrument sits between -1.21 and +1.22 with no consistent sign. The one thing that paid was the **feature set** - extremes, derivatives, the extremes behind, volatility - which took the turn-depth head from **0.0451 to 0.1894** on an identical series. Two silent harness defects recorded: FOCuS fed raw returns at `scale=1.0` clears no threshold ever and is one-sided, and a confirmed ZigZag pivot is **unbreakable before the next one confirms**, so two versions of the cycle test measured zero events by construction |
| [diverging.md](diverging.md) | **the same anti-signal found twice independently, and why inverting it is the trap** - a separate structural detector measured its most-emitted state at **n=410, 46.8% hit, -22.4 bps, t = -2.07**, with the right mechanism named: it reports the direction of the *exhausted* move, not the next one. This folder derived the identical mechanism for the z-score on Boom and Crash (hit **0.119 and 0.164**) without knowing of it. **A detector built to find exhaustion will label the exhausted move** - a defect class, not a bug. The inversion offered as the fix is circular: flipping a binary label negates every signed return by construction, so the -2.28 becoming +2.28 is arithmetic and contains no new information. Two things in the same evidence refute it - **every state** was negative including ones meaning opposite things (the sample fell, not the detector), and the single positive surface reads -20.5 and -79.8 on two others, so no global sign can be right. `calibrating.md` puts one pass of this desk's analysis at **63.05%** chance of an interval excluding zero on null data. What ships here instead defers to each feed's own scored record, so an anti-signal family simply never clears. Also carries the framing worth more than the detector - how probability is *formed*, which error diverges from it, who is on the other side - which answers a year of nulls in one line: **the synthetics have no participants**, so there is nobody to be wrong, and `deriving.md`'s theorem is the formal version of that |
| [closing.md](closing.md) | **Ramin Rostami's system, three parts sound and the load-bearing one invalid** - his argument runs: the market is zero-sum, therefore every cycle must close itself, structure is fractal so read 20-30 timeframes at once, the zigzag is the measuring instrument not the signal, cycles close at **86% of the previous leg**, and the path is a cubic-plus-exponential fitted by least squares and corrected by its own error. **Zero-sum does not imply mean reversion** - a random walk is a martingale, zero-sum by construction, and has no level to return to; zero-sum constrains the expectation of a bet, not the path of the price, and `deriving.md`'s `E[net] = -(c/2) x turnover` is what it actually buys. The **86% figure does not survive**: median retracement is **1.00 on every arm including Brownian motion**, half of all legs exceed the one before, and the band around 86% holds **0.0709 on gold against 0.0666 on a random walk**. What is right: the fitted-and-error-corrected path is `streaming.md`'s depth head, already built and the one component measuring anything; treating the detector as a measuring instrument is `delivering.md`'s discipline; and 200 parallel zigzags are affordable now that every part has an O(1) recursion. Where closure **is** real is where somebody is paid to close it - a cross-venue spread, AUC 0.6985 against 0.5031 |
| [break-trade.md](break-trade.md) | **measured on 312,420 live journal resolutions - one factor of three survives, and a published figure corrected** - the desk's construction was probability x magnitude x hold time. **Magnitude is a clean null**: seven causal features against the realised move, every correlation inside **+/-0.006**. **Probability works** - `experience` and `strength` read inverted are AUC **0.624 and 0.608** unconditionally, ahead of anything in `force.md`, and the ranking *inverts* between the 300-1,800s band and the population, which is Simpson's paradox for the fourth time in this folder. **Hold time works, reframed**: not as an up-front forecast but as a live clock - `P(break | still open at t)` runs **14.4% -> 77.8%** on 1m and crosses even money at **four to seven bars of the level's own timeframe** on every timeframe from 1m to 30m, a thirtyfold span. Elapsed time is the one conditional that costs nothing to observe, since "resolution took at least t" and "still open at t" are the same event. **The correction**: `interval_log`'s published 1m break rate of 57.9% is a selection effect - measured only on resolutions lasting five minutes or more, while the median 1m touch resolves in **120 seconds**; unconditionally it is **13.4%**. So the tempting story - that the desk's -7.75 per close at 1m is it taking the wrong side of a 58/42 split - is **wrong**, and the 1m losses are cost, not side. The harness's expectancy columns are flagged contaminated: `push_vol` is measured to a resolution point *defined* by distance travelled, which `Touch.path` already documents as an identity rather than a forecast |
| [signal-registry.md](signal-registry.md) | **shipped** - every candidate in this folder at or above 51%, audited, with the seven reachable ones computing live in `structures/context/registry.py`. Each arrives with its **backtested** claim and is then scored on **live** resolutions, and ensemble weight comes from the live separation rather than the claim - so a decayed signal loses its vote without anyone editing a table, which is the thing this folder has never been arranged to notice. Excluded with reasons: the cross-venue spread at AUC 0.70 (**unreachable** - needs an account per venue, and a sub-second half-life), `turns.md` at 0.595 (real, unbuilt, daily clock against a desk that now trades 1m-30m), `strength.md` at +32.8 (**measured while `vol.bps` was divided by the venue count**, so the labels are affected and not only the covariates), the synthetic sequence models at 0.53-0.58 (**provably null** - a Volatility index is GBM at a published sigma), and the analogue twins at 50.0-50.6% (below the bar). Also records the bar itself: **51% is below the cost line** - 0.02 of a unit gross against a 0.040 TR round trip - so nothing at 51% pays as a trade and the registry does not trade them. And two live-environment corrections: `TRADING_MAX_BREAK_RISK=0.35` **is set on the instance**, so `Breaks` is not "deciding nothing" as its own comment says, and the one defect worth more than the whole table is `winning.md`'s pre-registered survivor still measured against **reward** where it was validated against **risk** - 49 trades, -0.471R each |
| [linear-algebra.md](linear-algebra.md) | **measured, three questions and three answers** - eigenvalues, hidden states and the perceptron, which are one subject: how much of the structure in a matrix is real. **The book has one factor.** Seven instruments, 20,222 shared hourly bars: eigenvalues 3.194, 1.273, 0.984, ... and the first carries 45.6% of variance with an eigenvector that is **the dollar**, not a generic market mode (gbpusd -0.47, eurusd -0.47, usdcad +0.40). Validated in both directions against Marchenko-Pastur - **2 factors on the real book, 0 on the synthetics and 0 on pure noise** - which works because `generated.md` established the synthetics share no driver, making them a known-null control. The usable number is the **window sweep**: at a rolling 50 bars one factor is already detectable (lambda_1 3.49 against a shuffled 1.56) and lambda_1 barely moves out to 5,000 bars, but the **second** factor needs thousands, so a rolling covariance on this book has one real degree of freedom and anything built on a third component is fitting noise. Top eigenvector overlap out of sample **0.995** against 0.378 for random directions. This also closes `metals_pair.py`'s open question - gold and silver correlate at +0.7631 with nothing at any lag **because both load on one factor**, so it is shared exposure rather than lead-lag and there is nothing in it to trade. **The regimes are real and useless.** A three-state Gaussian HMM, forward-**filtered** only (smoothed labels see their own future), passes the surrogate test that withdrew `families.md`'s three-state fit - dwell 3.05-3.30 against 1.49-2.53 on every control, all three instruments - because that withdrawal was measured on constant-sigma synthetics and real markets have volatility clustering. And the states predict **nothing**: direction 0.4912-0.4983, *below* a coin, with the smoothed look-ahead worth under 0.003 (so published regime results are not explained by that leak - there is no directional signal to leak), and the volatility forecast losing to a 100-bar trailing sigma by 3% on BTC to 72% on eurusd. **The perceptron reaches the margin and the margin is negative.** Over 192,402 touches the mistake curve runs 0.138 -> 0.219 -> 0.189, up then flat, never converging: `R` = 420.2 and the minimum margin is **-91.3**, so Novikoff's bound does not exist and no plane separates the classes. The perceptron is **worse than always-hold** (0.1895 against 0.1240) because a mistake-driven update chases a 12.4% minority; the smooth loss is worth **+0.074 AUC** (0.658 against 0.584); and whitening onto the eigenvectors moves nothing (0.1895 -> 0.1900), so the features are not ill-conditioned, they are not separable. That bounds every linear model including production's, and is the mechanism behind `models.md`'s 1KB logistic beating a forest and an MLP. **An independent replication**: this harness's online logistic scores AUC 0.6582 unconditionally against `force.md`'s 0.658 in-band from different code - reassuring given that the individual features' AUCs invert between band and population. Two web sources supplied for this were checked and neither treats eigenvalue significance at all; one's worked yield-curve PC2 at 14.4% of 8 maturities is an eigenvalue of 1.15 against a noise band reaching 1.29, i.e. **inside it** |
| [news-volatility.md](news-volatility.md) | **measured, and the first large positive result here in a long while - shipped** - the economic calendar predicts volatility, net of the session. 191 instrument-events on eurusd/gbpusd/xauusd at 15m against a control of the same instrument, same hour of day, same weekday, within 120 days, no event inside two hours: **0.789x in the two hours before a print, 2.424x on the print's own bar, 1.762x at +30m, 1.349x at +1h, 1.120x at +2h and 0.920x at +4h** - every interval excluding 1.0 and both halves of the sample agreeing in sign at every horizon. Three findings: the spike is real and short; it **overshoots then undershoots**, ending *below* normal by four hours, which is not in the folklore; and **the lull is the part worth having** - least obvious, knowable furthest ahead, and a 21% change in a volatility-scaled sizing input. **Why it works where direction never has**: a release time is published days in advance, so the timing carries no estimation and there is no look-ahead to argue about. **The confound that decides it**: 61 of 161 high-importance events land in the 12:30 UTC hour, which is also the London/NY overlap - against an all-hours baseline this study returns a big, confident, spurious number. **A second confound, caught because gold misbehaved**: drawing controls from four years of history while events span six weeks made gold's *pre*-release window read 1.431x, which is not a market doing anything, it is gold's recent level above its own four-year mean; localising the control pool fixed it to 0.905 - so a ratio against a long baseline measures level drift plus effect, and the effect is the smaller term. **usdjpy is excluded and unexplained** at 5.0x on the print and 1.902x before it; the obvious explanation was tested and **failed** - dropping all 22 floating-time events (rate decisions, statements, press conferences) left it at 1.902x unchanged. **The surprise is unresolved**: |actual-forecast| correlates +0.091 with the first hour's volatility while the decile split runs the other way (loudest 30% moved 1.226x, quietest 1.692x), because scaling by \|forecast\| is unstable near zero and 144 events over dozens of titles leaves no per-indicator history to normalise against - and the surprise is known only at release, so it could only ever size after a print rather than anticipate one. Shipped as `structures/context/releases.py` with 16 tests, publishing `release_minutes`, `release_vol_multiple` and `release_vol_log` and deciding nothing; the sign of `release_minutes` is pinned by a test because reading it backwards would size **up** into the quietest window of the day, and the +4h undershoot is deliberately **not** published since it describes a cumulative window rather than an instant |
| [crash-timing.md](crash-timing.md) | **measured - a crash can be timed and not signed, so the score is a risk switch.** The odds of a bar beyond 4x baseline are very predictable - AUC **0.75 / 0.65 / 0.72** at 15m / 1h / 1d with four trailing features, 0.81 boosted, lift about 4x in the top 5% - but the crash model ranks melt-ups as well as crashes (15m 0.809 vs 0.780; on daily it predicts melt-ups *better*, 0.760 vs 0.693), and the sign of a coming large bar is AUC 0.56-0.57 at every interval across 42 features including OHLC wicks, Elliott-style impulse counts, critical slowing down, permutation entropy, fractal dimension, LPPL curvature and Hawkes intensity. Only `downshare` (downside share of variance) and, at 1h, long lower wicks point at crashes. **Shorting the alarm is null intraday and wrong-signed on daily** (-0.77 +- 0.20 baseline units): the buy-the-fear effect. **The value is on the risk side**: the top fifth of the score carries a 34% chance of a 4x bar against 2.1% in the bottom, and a 3x wider adverse excursion; halving size there cuts the 1% tail 14-26% at neutral Sharpe (worse at 1h). **Wicks decide stops**: a resting stop is hit about 1.4x as often as closes say, a close-based exit's 1% tail is -2 to -3.9R against -1R, and in high-score windows a 1U stop is hit six times in ten - widen and halve there. Also: **yahoo's daily FX bars leak tomorrow** (close location predicts the next return at -0.44 against +0.002 on OANDA), and `spikerisk.load` now refuses any series that fails that check |
| [directional-questions.md](directional-questions.md) | **measured, null, and three defects that were the only positives** - a stop-and-target race over 1h-20d instead of the next bar, six sharper questions (swing continuation, pair spreads, event-conditioned races, 3:1 and 1:2 payoffs, limit-vs-market, excursion shape) over 148 cells with **none** surviving three standard errors in both halves, and a 57-92 signal ensemble over price families, VIX term structure, a leave-one-out dollar index, OHLCV order flow (bulk volume classification, VPIN, Amihud), fair value gaps, event models and pair spreads - 15-22 eigenvalues above the Marchenko-Pastur edge, 48-68 independent clusters, and **no combination (equal, inverse-covariance, eigenvector, stacked, regime-switched, confluence) beating the better of always-long and always-short** at any interval. Waiting for 70% of independent views to agree happened on **0% of bars**. Varimax-rotated, **each independent component is one information source across all its horizons** - VIX, calendar, borrowed fields, volatility, dollar, order flow, FVG, technical, bar shape, cross-asset, pairs and each event type - so horizons add almost nothing independent; VIX validates best (IC +0.06) and fails on test at every interval, and order flow is the only source positive on both blocks at all three (+0.003 to +0.039, within two standard errors). The positives were defects: stops booked at the barrier instead of the crossing close (six fake 3:1 survivors), stops read from closes instead of wicks, and yahoo daily bars that leak tomorrow - which also killed the day's best lead, a daily long model that beat holding in 14 of 15 years and on clean data loses (-0.15R, 2 of 15 years). **One input changed outcomes**: trades holding through a high-importance release did worse in 32 of 36 ensemble rows, by a median 0.19R. Leads to re-test, not trade: intraday short models at 1-5h (+0.12 to +0.16R gross), daily 3:1 longs (+0.10R, 2.9 SE), daily pair-spread reversion (+0.11R, 2.0 SE) |
| [susceptibility.md](susceptibility.md) | **measured - one borrowed concept pays, one is refuted, and a positive control validates the instrument** - Ising susceptibility `chi = var(M)` where `M` = net returns / total \|returns\|, which **is** `trend.py`'s efficiency ratio already in production, so the borrowed quantity is its *variance*, and the physics is a theorem: fluctuation-dissipation makes chi proportional to var of the order parameter, which peaks at a phase transition. **On the volatility level it is null** - 2 to 5x worse than a trailing sigma on QLIKE, correlation with forward volatility never past 0.061, incremental R-squared 0.0008-0.0085 - and it carries the folder's clearest lesson on effect size: a t-statistic of **31.8** beside a dR2 of **0.0085**, with a coefficient that **changes sign with the horizon**. **The leading-indicator claim is refuted and inverted, and the inversion holds**: after a loud chi, volatility **falls** 5 to 9 points more often at 20-60 bars on 5 of 7 instruments - and it survives the control that should have killed it, staying at +5.3% to +9.1% *within deciles of current volatility*, so it is not volatility mean reversion wearing a physics name. **The synthetics confirm it**: a Volatility index is GBM at constant sigma so there is no volatility to forecast, and the method duly reads -1.1% to +0.3% there - skill on real markets, none on processes that have none. **The semivariance decomposition validates beautifully**: Barndorff-Nielsen's RS+/RS- asymmetry reads **+0.136/+0.184 on Boom and -0.136/-0.188 on Crash**, mirror instruments with a designed one-sided jump, and -0.002 on the symmetric indices. With the measure validated, chi predicts neither side (r = -0.022 to +0.018) nor either side's level (dR2 <= 0.0062), and the desk's hypothesis that *trend up plus expected volatility resolves downward* comes out **-3.2% to +2.7%**, inconsistent in sign - null. **Fatigue is refuted in its stated direction**: the borrowed claim is that more touches means readier to break; over 320,811 touches the break rate falls **15.0% -> 12.1% -> 11.3% -> 8.8% -> 2.9%** across experience quintiles, monotone and fivefold, AUC 0.6272 read inverted. The mechanism is survivorship - a level accrues touches only by holding - which does not invalidate the prediction but does mean the **decayed** variant is a different question and remains untestable: the journal's outcome rows carry no level identifier (`level_price` absent from all 323,712), so a per-level recency-weighted count needs a join to the call entries first. Also noted: their **CFAR** adaptive threshold is the signal-processing name for the homeostatic rate regulation `changepoint.py` reached independently, where a fixed threshold fired 0.06 vs 43.16 per thousand bars across regimes against a regulated 2 |
| [break-features.md](break-features.md) | **measured - the three that worked were already in the feature set and being ignored, and all four that needed something new are null** - `Breaks` fitted six inputs and now fits nine. **`experience` (AUC 0.6272) and `strength` (0.6076)** were excluded because `force.md` measured them inside the 300-1,800s band where they lean the *wrong* way; the band keeps only resolutions of five minutes or more while the median 1m touch resolves in **120 seconds**, so it discards most of the population non-randomly - the features were never the confound, the conditioning was. `experience` is monotone across five quintiles, **15.0% to 2.9%**. **`conviction` = \|up_rate - 0.5\| (0.6006)** is the interesting one: `up_rate` really is worthless as a *direction* (0.5005 here, confirming the 0.4892 it was excluded on) but a linear model **cannot represent \|x - 0.5\| from x**, so it was dropped for a property of the model rather than of the feature - the same failure the file already documents for the slope sign. Hold rate rises 83.8%, 88.2%, 92.0%, 92.8% with conviction, and it is **not** a restatement of experience despite r = +0.573: it separates inside every experience quartile, strengthening from 4.3 points at the bottom to **14.8** at the top, with a joint span of **3.1% against 18.4%**. That also settles a standing contradiction - `strength.md` had the same-side record at **+32.8 points** while `breaking.py` had it at AUC 0.4892; both are right, because they were scoring different readings of one feature. **Four nulls**, each joined to the bar at or before the touch over 28,675 touches: Ising `chi` 0.5259 (+1.4% lift inside experience quintiles), the **dollar factor** 0.5075, trailing semivariance asymmetry 0.5103, trailing return 0.5007 - and the **calendar says nothing about breaks**, 7.9%/9.4%/9.3%/8.5%/8.4% by minutes to a release against an 8.6% base with no ordering. So the calendar moves volatility and not the probability a level gives way, which is the same lesson `breaking.py` opens with. **The clock is not shipped into the model and cannot be**: `Features` is frozen at touch open by design. `registry.aged()` is the arithmetic that makes it usable and is tested - log-odds with the clock entered as a shift from its own base rate, so a fresh touch is unchanged - but both consumers are load-bearing (a per-quote hot path, and `max_break_risk` in the order path, live at 0.35 and **gating parked signals on a stale probability**, which is a real defect recorded rather than patched unreviewed) |
| [momentum-buildup.md](momentum-buildup.md) | **measured, null - the test a supplied paper could not run** - the desk handed over a Momentum Bar Build-Up paper specifying the hypothesis, ablation, universe and protocol, with its empirical component marked **pending** because the author's runtime could not reach raw OHLCV: *"a research paper containing invented backtest statistics is not a research paper, it is fan fiction wearing statistics."* This desk has the data, so this is that test - `M` a volatility-normalised weighted return sum, `V = dM`, `A = d2M`, the paper's nested B0-B6 ablation, one parameter set across four instruments and three horizons, chronological hold-out purged by the horizon, block-bootstrap intervals, 4bp charged. **Nine of twelve incremental R-squareds are negative** out of sample - the full MBB state does *worse* than `M + sigma` - the three positives are +0.00001, +0.00007 and +0.00024, and eleven of twelve lose roughly the spread. The single money-making cell is the instructive one: gold at twenty bars nets +0.67bp with a bootstrap floor reaching **-2.53**, so it fails its own interval, and one positive in twelve is below the 0.6 chance expects. **0 of 12 clear both bars.** Velocity prices at **-0.00006** on the B5-B4 step, so there is no improvement left for acceleration to explain a share of, and hit rates sit in 49.9-51.0% everywhere. **But the idea has a positive precedent here and it is worth being exact about it**: `slopes.md` measured `slope` *and* `prior_slope` at **+0.030 AUC on breaks** with precisely the MBB argument - *"a flat approach means one thing after quiet and another after a run"* - and that pair **is** a velocity term, already fitted and shipped in `learning/breaking.py`. So momentum trajectory predicts **whether a level gives way** and nothing about **which way price goes**, the same split `break-features.md` found for the calendar (volatility 2.42x, break rates untouched) and `breaking.py` opens with for `up_rate` |
| [turns-fast.md](turns-fast.md) | **measured - it does not port, and the original needs re-reading** - `turns.md`'s AUC 0.595 was the strongest *unbuilt* result here, on a 60-**day** horizon a desk entering at 1m-30m cannot use. Ported clause-for-clause to an hourly clock on seven instruments: **mean AUC 0.5457, 2 of 7 episode intervals excluding 0.5**, two instruments *below* it, and the one good cell (btcusd 0.684) rests on about seven positive events. So trend exhaustion is **not** scale-free, unlike the break hazard (even money at 4-7 bars of its own timeframe across a thirtyfold span) or breakout excursion (invariant at every quantile). **The finding that reaches further** came from chasing a first version that scored 0.66 - *better* than the daily. Two faults: the universe gate kept **94.1%** of bars, which is the series rather than the comparison set the question needs; and the label is a **fixed** drop of 20 per-instrument median move units, so current volatility predicts reaching it almost by construction. Against that label `vol` **alone** scores **0.608-0.679 on all seven instruments and beats the full model on all seven** - and `auditing.md` records `vol` alone at **0.604** on the daily original's own ten-signal scan, the same number in the same place. Scaling the threshold by *trailing* volatility removes the tautology and the effect falls to 0.5457. This does not refute `turns.md` - different clock, gate and normalisation - but the 0.595 should not be built on until re-run with a contemporaneous threshold, which is one line in its harness |

## "We know the generator" is true of one tier and not the others

This claim is load-bearing across [deriving.md](deriving.md),
[rebuilding.md](rebuilding.md), [quantising.md](quantising.md) and
[grounding.md](grounding.md), and it gets stated too broadly - including by me,
on 2026-09-12, to an agent that was about to score a learned object against
"ground truth". It holds in three tiers and only the first deserves the phrase.

**Tier 1 - the Volatility family and Step Index. Ground truth is real.** The
parameters are published rather than inferred, and verified independently:
realised volatility equals the name on 12 of 12 within **0.49%**, all twelve
within two standard errors over **916,248** comparisons, exactly Brownian at
H = 0.50 and kurtosis 3.00, Step Index a fair coin to **p = 0.499734 ±
0.000220** over 5.2M flips, and closed form, Monte Carlo and data agreeing to
**0.1-0.4%**.

**But even here it is failure-to-reject, not proof.** `rebuilding.md`'s `n*`
infinite means indistinguishable *at the power we have*, and its discriminator
floors at 0.48 rather than establishing identity. A method that finds structure
on a Volatility feed is a bug first and a discovery second - but it is allowed to
be the second, and saying so is the difference between a bound and a belief.

**Tier 2 - Boom and Crash. There is no ground truth; the published spec is
wrong.** Three defects, all measured: the published `lambda`, `E[g]` and `E[J]`
miss their own closure by **6-12% on all six feeds**, so a compound Poisson built
from them is not even a martingale; the grind **is not a gamma**, its published
two moments implying a density that is zero at the origin where the feed's is
flat; and the spread is a **second process that is not specified at all**. A
simulator built from that table is a falsified hypothesis, not a reference.
Build from the feed's own marginals - `rebuilding.md`'s `pool` arm is caught 0 of
24 where `spec` is caught 24 of 24 - and say that is what was done.

**Tier 3 - Range Break and Jump. Incomplete, and two routes disagree.** The
shared-60 width is withdrawn, RB200 does not converge, and the ladder excludes
the hard box at 3.4-4.2 sd while landing *below* the spring. An open question to
contribute to, not a truth to check against.

The shape of it, which is awkward and worth saying plainly: **ground truth is
solid exactly where no method can profit, and absent or moving exactly where the
specification is still being repaired.** So method *validation* belongs on tier
1, and the identification work is worth more than the prediction work on tiers 2
and 3.


Designs nobody has built and measurements nobody has taken live in
[planned/](planned/) - the one thing that must not sit beside documentation of
things that exist, because that is where a reader cannot tell the difference.

Findings that changed the code, or that belong next to it, are written up in
`docs/` instead - [edge.md](../docs/edge.md), [strength.md](strength.md),
[absorption.md](../docs/absorption.md), [magnet.md](../docs/magnet.md),
[news-dedup.md](../docs/news-dedup.md).

## Running the harness

From the repository root, with the stored prices database in place:

```bash
python research/harness/touches.py    # replay once, cache resolved touches
python research/harness/models.py     # model and similarity comparison
python research/harness/edge_gate.py  # the |edge| gate, thresholds and quantiles
python research/harness/similarity.py # does feature distance predict agreement
python research/harness/features.py   # feature importance, and generated features
python research/harness/holds.py      # the edge against "assume the level holds"
python research/harness/vol.py        # does the volatility estimate predict the next move
python research/harness/cycles.py     # does cyclical context change what a touch means
python research/harness/turns.py      # can a major turn be seen before it happens
python research/harness/prior.py      # what edge measures, and whether the kNN earns its place
python research/harness/magnitude.py  # expected_push, risk_vol and the reward-to-risk gate
python research/harness/topology.py   # the transit graph over levels
python research/harness/structure.py  # confluence, the level set's shape, and volatility
python research/harness/crossing.py   # cross-venue deviation, FX triangles, cross-instrument lead-lag
python research/harness/states.py     # does a level's behaviour change over its life
```

The generator study runs on the lab rather than here - it reads `research.db`,
which is 60 days of bars and 24 hours of ticks across 53 feeds:

```bash
./.secrets/lab.sh run research/harness/genvol.py    # realised vs nominal volatility, clustering
./.secrets/lab.sh run research/harness/genstep.py   # Step Index: fixed step, fair coin, barriers
./.secrets/lab.sh run research/harness/genspike.py  # Boom/Crash: spike rate, grind, the arithmetic
./.secrets/lab.sh run research/harness/genrange.py  # Range Break: break rate, fade, follow-through
./.secrets/lab.sh run research/harness/genstop.py   # what a stop is worth when the adverse tick jumps
```

The rebuild in [rebuilding.md](rebuilding.md) runs on the same machine and reads
the same database. `rebuildgen.py` is the generators and the battery and is
imported rather than run:

```bash
./.secrets/lab.sh run research/harness/rebuildvol.py MULT=10 JMULT=4  # GBM and Jump, rebuilt
./.secrets/lab.sh run research/harness/rebuildstep.py MULT=10 RBMULT=5 # Step and Range Break
./.secrets/lab.sh run research/harness/rebuildspike.py BMULT=2        # Boom and Crash
./.secrets/lab.sh run research/harness/rebuildjudge.py                # can anything tell them apart
./.secrets/lab.sh run research/harness/rebuildpower.py                # at what n does a test separate
```

`touches.py` writes `touches.pkl` beside itself and the others read it, so the
replay runs once rather than four times.

## Which data these were measured on

Everything dated **2026-08-16** was re-run on 10,484 touches across fourteen
instruments, after the backfill in [todo.md](../docs/todo.md) §0d took the
store from 455k bars to 1.56M. The earlier readings used 1,995 touches across
six instruments over days rather than months, and are in git.

Two of the six documents changed their answer. [turns.md](turns.md) went from
"does not separate from chance" to AUC 0.595; [cycles.md](cycles.md) went from
"nothing separates anywhere" to one marginal cell. The other four reproduced,
with absolute accuracies about five points lower on the harder sample and the
rankings intact.

**Re-run the harness after any backfill.** Every document here reads
`touches.pkl`, which regenerates from whatever is in the store, so a collection
run silently invalidates all of them.

## What every result here shares, and its limits

**Walk-forward.** Nothing is predicted from its own outcome or from anything
that resolved after it.

**Bars only.** Production drives touches from quotes as well, and on
2026-08-14 that difference twice overturned a replay result: the
instant-resolution fix looked complete on a replay and was still 42.9% wrong
live. Treat every number here as the level machinery rather than the system.

**One replay, six instruments, ~2,000 touches.** Enough to rank things, not
enough to pin a constant. Where a number is quoted as transportable, the
document says so and shows the split that establishes it.

**Measured after 2026-08-14.** Everything before that date was counted on data
where roughly half of all outcomes were artefacts - touches resolving at the
instant they opened - so earlier measurements in this project are not
comparable, and several were withdrawn.

## Module notes

One file per module, holding what used to sit inside its docstrings: the
measurement behind a number, the failure a guard came from, the thing that was
tried and did not work. Moved out on 2026-09-19, when the package was 47%
prose by line - 25,948 lines of it against 29,562 of code - and the file you
opened to find out what a function *did* answered with its own history first.

The code kept every summary line and gained a pointer. Nothing here is new and
nothing was discarded; `git log -p` on the module is the same text in the same
order. Named after the module, so `trading/service.py` is
[trading-service.md](trading-service.md).

They are notes rather than documents: no argument runs across them, and a
section is only worth reading beside the function it names.

