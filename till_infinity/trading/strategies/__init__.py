"""Where a trade is decided.

Grouped into a package on 2026-09-04. `trading` had grown to 32 modules
and 14,852 lines in one flat directory; nothing here is persisted, so
unlike `structures` these could be moved without breaking a pickle.
"""
